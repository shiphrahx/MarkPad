node_modules/
dist/
src-tauri/target/
src-tauri/gen/

.env
.env.*

coverage/
*.tsbuildinfo
.vite/

.DS_Store
Thumbs.db

# Icons are generated from scripts/make-icon.mjs, not committed. The repo rules
# say no binaries, and `pnpm icons` rebuilds them from source in one step.
#
# Anchored. Unanchored it matched any directory called build anywhere in the
# tree, which quietly swallowed a tests/build folder that had nothing to do
# with icons.
/build/
src-tauri/icons/
